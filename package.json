{
  "name": "superauction",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "dev": "nodemon -e sol,js -i build -x 'npm test 2>&1'",
    "build": "run build:*",
    "build:contracts": "rm -f build/contracts/*;truffle compile",
    "test": "run test:*",
    "test-coverage": "npm build && truffle run coverage --file test/contracts.test.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "3.3.0",
    "@superfluid-finance/ethereum-contracts": "^0.2.3",
    "@superfluid-finance/js-sdk": "^0.2.2"
  },
  "devDependencies": {
    "@decentral.ee/web3-helpers": "^0.3.5",
    "@openzeppelin/test-helpers": "^0.5.10",
    "ganache-time-traveler": "^1.0.15",
    "solidity-coverage": "^0.7.14"
  }
}
